---
title: "Individual Assignment 2: Django MVT Implementation"
format:
  html: default
  typst: default
  docx: default
format-links:
  - html
  - format: typst
    text: PDF
    icon: file-pdf
  - docx
---

::: {.content-visible when-profile="id"}

# Individual Assignment 2: Implementasi Model-View-Template (MVT) pada Django {.pbp-page-title}

Pemrograman Berbasis Platform (CSGE602022) - diselenggarakan oleh Fakultas Ilmu Komputer Universitas Indonesia, Semester Gasal 2026/2027

**Kontributor:** Alvin Christian Halim

***

::: {.callout-important}
### Tutorial 02 adalah Prasyarat Wajib
Individual Assignment 2 **hanya dinilai apabila Tutorial 02 sudah diselesaikan** paling lambat **Rabu, 9 September 2026**. Kalau Tutorial 02 belum dikerjakan, Individual Assignment 2 **tidak akan dinilai**, meskipun sudah dikumpulkan lewat SCELE.
:::

## Tentang Proyek

Proyek Individu mata kuliah ini adalah sebuah **website portofolio pribadi** yang dikerjakan sendiri. Individual Assignment dirilis tiap minggu mengikuti topik Tutorial minggu itu, dan setiap assignment melanjutkan langsung dari kode yang kamu buat di Tutorial pada minggu yang sama.

## Target Tugas Ini

Pada [Tutorial 02](../../tutorial/tutorial-2.qmd), kamu sudah menerapkan pola Model-View-Template (MVT) untuk menampilkan data pengalaman (*experience*) pada portofoliomu. Pada tugas ini, terapkan kembali alur yang sama untuk **satu bagian lain** dari portofoliomu, misalnya daftar proyek, pendidikan, sertifikasi, atau bagian lain yang relevan.

Bagian baru tersebut harus memiliki halaman sendiri, terpisah dari halaman utama yang memuat bagian *experience*, dan dapat dibuka melalui *navbar*.

::: {.callout-note}
Tugas ini hanya mewajibkan **halaman daftar** untuk bagian yang kamu pilih. Halaman detail bersifat opsional dan dapat menjadi fitur tambahan.
:::

Checklist minimal untuk tugas ini:

- [ ] Tambahkan satu model baru pada aplikasi `main` yang merepresentasikan bagian portofolio pilihanmu.
- [ ] Model tersebut memiliki minimal tiga *field* selain *primary key* (`id`, baik dibuat otomatis oleh Django maupun ditentukan sendiri), dengan tipe data yang sesuai.
- [ ] Buat dan terapkan migrasi model, lalu sertakan berkas migrasinya dalam commit.
- [ ] Buat sebuah *view* yang mengambil data dari model, memasukkannya ke dalam *context*, dan meneruskannya ke *template* baru.
- [ ] Tampilkan seluruh objek menggunakan perulangan Django Template Language dan sediakan tampilan untuk kondisi ketika data masih kosong.
- [ ] Data pada bagian portofolio baru tidak ditulis langsung (*hard-coded*) di HTML. Teks antarmuka statis, seperti judul halaman, label navigasi, dan isi *footer*, tetap boleh ditulis di *template*.
- [ ] Daftarkan *named route* pada `main/urls.py` dengan URL yang berbeda dari halaman utama.
- [ ] Tambahkan tautan menuju halaman baru pada *navbar* menggunakan tag `{% url %}`. Pastikan *navbar* dan *footer* konsisten dengan halaman lain.
- [ ] Tambahkan *unit test* yang memastikan URL dapat diakses, *template* yang tepat digunakan, dan data dari model tampil pada respons HTML.
- [ ] Pastikan proyek dapat dijalankan dengan `python manage.py runserver` tanpa *error* dan seluruh test lulus ketika menjalankan `python manage.py test`.

## Pertanyaan Reflektif

1. Jelaskan alur yang terjadi ketika pengguna membuka halaman portofolio baru, mulai dari permintaan yang diterima proyek hingga data ditampilkan pada *browser*. Dalam jawabanmu, jelaskan peran `urls.py` proyek, `urls.py` aplikasi, *view*, model, dan *template*.
2. Mengapa data untuk bagian portofolio baru sebaiknya disimpan pada model dan tidak ditulis langsung di dalam *template*? Jelaskan dampaknya terhadap kemudahan pemeliharaan dan pengembangan aplikasi.
3. Apa perbedaan fungsi `makemigrations` dan `migrate` pada Django? Berikan contoh perubahan model yang mengharuskanmu menjalankan kedua perintah tersebut.

Jawab pertanyaan-pertanyaan di atas di `README.md` proyekmu (bukan di halaman tugas ini), dengan format berikut supaya tetap rapi seiring bertambahnya pertanyaan reflektif tiap minggu sepanjang semester:

```markdown
### Tugas 2

1. ...
2. ...
3. ...
```

## Pengumpulan

Tugas dikumpulkan lewat slot submisi yang disediakan di SCELE dalam bentuk **satu tautan ke commit** di GitHub, bukan sekadar tautan repositori. Kumpulkan tautan ke **commit paling akhir yang sudah di-*push* sebelum tenggat waktu** dan menunjukkan hasil akhir tugas ini. Commit tersebut menjadi batas riwayat yang akan diperiksa oleh asisten dosen. Commit yang di-*push* setelah tenggat waktu tidak akan diterima. Pastikan repositori GitHub kamu bersifat **publik** supaya asisten dosen dapat mengaksesnya untuk penilaian.

## Tenggat Waktu Pengerjaan

Individual Assignment 2 dirilis pada **7 September 2026** dan dikumpulkan paling lambat **14 September 2026, pukul 12.00 WIB**. Tenggat waktu ini lebih lama daripada tenggat Tutorial 02, yaitu Rabu, 9 September 2026.

## Rubrik Penilaian

### Fungsionalitas & Kesesuaian Topik (70%)

- **1** - Tugas belum lengkap; aplikasi gagal berjalan (*crash*/error 500); sama sekali gagal mengimplementasikan materi minggu ini.
- **2** - Aplikasi berjalan sebagian; banyak fitur *error*; implementasi materi minggu ini minim atau salah.
- **3** - Aplikasi berjalan baik dan berhasil mengimplementasikan materi minggu ini, dengan sedikit bug atau fungsi yang kurang mulus.
- **3.5** - Fungsionalitas berjalan sempurna tanpa bug. Semua instruksi dan topik minggu ini diimplementasikan dengan akurat.
- **4** - Menambahkan fitur ekstra, interaktivitas, atau elemen UI/UX yang relevan di luar instruksi minggu ini.

### Kualitas & Struktur Kode (10%)

- **1** - Kode sangat berantakan; logika tidak jelas; terlihat seperti *copy-paste* tanpa memahami struktur yang mendasarinya.
- **2** - Kode terbaca tapi mengabaikan konvensi *framework* (mis. penamaan variabel acak, kode *spaghetti*).
- **3** - Kode cukup rapi dan terstruktur, mengikuti alur Model-View-Template serta *routing* yang diajarkan di Tutorial 02.
- **3.5** - Struktur kode sangat rapi dan modular; tanggung jawab model, *view*, *template*, dan konfigurasi URL dipisahkan dengan jelas; serta test mudah dibaca.
- **4** - Menerapkan optimasi kode tingkat lanjut, memisahkan logika dengan bersih, atau menambahkan komentar/docstring kode yang sangat informatif.

### Git & Disiplin (10%)

- **1** - Dikumpulkan terlambat; Git sama sekali tidak dipakai (hanya unggah ZIP/berkas manual).
- **2** - Dikumpulkan tepat waktu; penggunaan Git sangat buruk (mis. cuma 1 *commit* besar dengan pesan tidak jelas).
- **3** - Dikumpulkan tepat waktu; ada beberapa *commit*, tapi pesan *commit* tidak deskriptif (mis. "update", "fix").
- **3.5** - Dikumpulkan tepat waktu. Riwayat *commit* rutin, mencerminkan progres bertahap, dan pesannya sangat deskriptif.
- **4** - Menerapkan *branching* yang terorganisir untuk tiap fitur, atau menjaga riwayat *commit* yang rapi dan profesional (mis. *conventional commits*).

### Dokumentasi & AI Disclosure + Pertanyaan Reflektif (10%)

- **1** - `README.md` tidak disediakan; sama sekali tidak menyebutkan penggunaan AI.
- **2** - `README.md` ada tapi tidak ada deskripsi proyek atau instruksi *setup* yang jelas. AI *disclosure* sangat samar (mis. "saya pakai ChatGPT").
- **3** - `README.md` terstruktur dengan deskripsi proyek dan *setup* yang jelas. AI *disclosure* menyebutkan *tools* yang dipakai dan secara umum menyebutkan bagian mana yang dibantu AI.
- **3.5** - `README.md` rapi dan terstruktur dengan instruksi *setup* mingguan yang jelas. AI *disclosure* transparan menjelaskan *tools*, strategi *prompting*, dan bagian spesifik yang dibantu. Menyertakan *chat* AI atau log *prompting*.
- **4** - Dokumentasi berkualitas *open-source* (termasuk progres mingguan). AI *disclosure* menganalisis secara kritis keterbatasan AI dan menyoroti perbaikan manual yang dilakukan.

:::

::: {.content-visible when-profile="en"}

# Individual Assignment 2: Implementing Model-View-Template (MVT) in Django {.pbp-page-title}

Platform-Based Programming (CSGE602022) - organized by the Faculty of Computer Science, Universitas Indonesia, Odd Semester 2026/2027

**Contributor:** Alvin Christian Halim

***

::: {.callout-important}
### Tutorial 02 Is a Mandatory Prerequisite
Individual Assignment 2 **is only graded if Tutorial 02 has been completed** by **Wednesday, September 9, 2026** at the latest. If Tutorial 02 has not been completed, Individual Assignment 2 **will not be graded**, even if it has been submitted through SCELE.
:::

## About the Project

This course's Individual Project is a **personal portfolio website** completed individually. Individual Assignments are released weekly according to that week's Tutorial topic, and each assignment continues directly from the code you wrote in the Tutorial for the same week.

## This Assignment's Target

In [Tutorial 02](../../tutorial/tutorial-2.qmd), you applied the Model-View-Template (MVT) pattern to display your portfolio's experience data. In this assignment, apply the same flow to **one other part** of your portfolio, such as a project list, education, certifications, or another relevant section.

The new section must have its own page, separate from the main page that contains the experience section, and must be accessible from the navbar.

::: {.callout-note}
This assignment only requires a **list page** for your chosen section. A detail page is optional and may count as an additional feature.
:::

Minimum checklist for this assignment:

- [ ] Add one new model to the `main` application that represents your chosen portfolio section.
- [ ] The model has at least three fields besides its primary key (`id`, whether created automatically by Django or defined explicitly), using appropriate data types.
- [ ] Create and apply the model migration, then include the migration file in your commit.
- [ ] Create a view that retrieves data from the model, places it in a context, and passes it to a new template.
- [ ] Display every object with a Django Template Language loop and provide an empty state for when no data is available.
- [ ] Data in the new portfolio section is not hard-coded in the HTML. Static interface text, such as the page title, navigation labels, and footer content, may remain in the template.
- [ ] Register a named route in `main/urls.py` with a URL distinct from the main page.
- [ ] Add a navbar link to the new page using the `{% url %}` tag. Keep the navbar and footer consistent with the other pages.
- [ ] Add unit tests that verify the URL is accessible, the correct template is used, and model data appears in the HTML response.
- [ ] Make sure the project runs without errors with `python manage.py runserver` and that all tests pass with `python manage.py test`.

## Reflective Questions

1. Explain what happens when a user opens the new portfolio page, starting from the request received by the project until the data appears in the browser. In your answer, explain the roles of the project's `urls.py`, the application's `urls.py`, the view, the model, and the template.
2. Why should the data for the new portfolio section be stored in a model instead of being written directly in the template? Explain how this choice affects application maintenance and future development.
3. What is the difference between `makemigrations` and `migrate` in Django? Give an example of a model change that requires you to run both commands.

Answer the questions above in your project's `README.md` (not this assignment page), using the following format so it stays organized as more reflective questions get added each week throughout the semester:

```markdown
### Assignment 2

1. ...
2. ...
3. ...
```

## Submission

Submit this assignment through the SCELE submission slot as **one link to a GitHub commit**, not just a repository link. Submit the **latest commit that was pushed before the deadline** and represents the final result of this assignment. That commit becomes the endpoint of the history reviewed by the teaching assistants. Commits pushed after the deadline will not be accepted. Make sure your GitHub repository is **public** so the teaching assistants can access it for grading.

## Deadline

Individual Assignment 2 is released on **September 7, 2026** and is due no later than **September 14, 2026, 12:00 WIB**. This deadline is later than Tutorial 02's deadline on Wednesday, September 9, 2026.

## Grading Rubric

### Functionality & Topic Alignment (70%)

- **1** - Assignment is incomplete; application fails to run (*crashes*/error 500); completely fails to implement the week's material.
- **2** - Application runs partially; multiple features error out; implementation of the week's material is minimal or incorrect.
- **3** - Application runs well and successfully implements the week's material, with minor bugs or clunky functions remaining.
- **3.5** - Functionality runs perfectly without bugs. All instructions and topics for the week are implemented accurately.
- **4** - Adds relevant extra features, interactivity, or UI/UX elements beyond the week's instructions.

### Code Quality & Structure (10%)

- **1** - Code is highly chaotic; logic is unclear; appears to be *copy-pasted* without understanding the underlying structure.
- **2** - Code is readable but ignores *framework* conventions (e.g., random variable naming, *spaghetti* code).
- **3** - Code is fairly neat and structured, following the Model-View-Template flow and routing taught in Tutorial 02.
- **3.5** - Code is highly neat and modular; model, view, template, and URL configuration responsibilities are clearly separated; and tests are easy to read.
- **4** - Implements advanced code optimization, cleanly separates logic, or adds highly informative code comments/docstrings.

### Git & Discipline (10%)

- **1** - Submitted late; Git is not used at all (only uploaded ZIP/manual files).
- **2** - Submitted on time; Git usage is very poor (e.g., only 1 massive *commit* with an unclear message).
- **3** - Submitted on time; multiple *commits* exist, but commit messages are not descriptive (e.g., "update", "fix").
- **3.5** - Submitted on time. Commit history is routine, reflects gradual progress, and messages are highly descriptive.
- **4** - Implements organized *branching* for individual features, or maintains a flawless and professional commit history (e.g., *conventional commits*).

### Documentation & AI Disclosure + Reflective Questions (10%)

- **1** - No `README.md` provided; no mention of AI usage whatsoever.
- **2** - `README.md` exists but lacks project description or clear *setup* instructions. AI disclosure is extremely vague (e.g., "I used ChatGPT").
- **3** - Structured `README.md` with clear project description and setup. AI disclosure lists tools used and generally mentions which parts were assisted.
- **3.5** - Neat, structured `README.md` with clear weekly setup instructions. Transparent AI disclosure explaining the tools, prompt strategy, and specific assisted parts. Includes AI chat or prompting history/log.
- **4** - *Open-source* quality documentation (includes the weekly progress). AI disclosure critically analyzes the AI's limitations and highlights manual fixes.

:::
